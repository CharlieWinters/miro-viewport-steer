appName: Viewport Steer
sdkVersion: SDK_V2
# Deployed on GitHub Pages. For local dev, temporarily set this back to
# http://localhost:3000 (and in the Developer Dashboard).
sdkUri: "https://charliewinters.github.io/miro-viewport-steer/"
scopes:
  - boards:read
  - boards:write
